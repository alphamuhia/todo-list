specVersion: "v1beta"
serviceId: "todo-list"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "todo-list-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
